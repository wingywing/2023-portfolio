name: Send Webmentions for Published Posts

on:
  push:
    branches: [main]
    paths:
      - 'src/notes/**.md'
      - 'src/writing/**.md'
      - 'src/projects/**.md'

jobs:
  send-webmentions:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Send Webmentions
        run: |
          DOMAIN="https://wingpang.com"

          # Find changed content files
          FILES=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -E '^src/(notes|writing|projects)/.*\.md$')

          if [ -z "$FILES" ]; then
            echo "✅ No relevant content changes."
            exit 0
          fi

          for FILE in $FILES; do
            echo "📄 Checking $FILE"

            if ! grep -q '^published:\s*true' "$FILE"; then
              echo "⏭️ Skipping: Not published"
              continue
            fi

            # Determine post URL
            RELATIVE=${FILE#src/}
            SECTION=$(echo "$RELATIVE" | cut -d/ -f1)
            SLUG=$(basename "$RELATIVE" .md)
            POST_URL="$DOMAIN/$SECTION/$SLUG"

            # Determine target (reply, like, bookmark, or bridgy)
            TARGET=$(grep -E '^(in-reply-to|like-of|bookmark-of):' "$FILE" | head -n1 | sed 's/.*:[[:space:]]*//')

            if [ -z "$TARGET" ]; then
              TARGET="https://fed.brid.gy/"
            fi

            echo "🔗 Sending Webmention: $POST_URL → $TARGET"

            # Try Bridgy Fed
            curl -s -X POST https://fed.brid.gy/webmention \
              -d "source=$POST_URL" \
              -d "target=$TARGET" \
              && echo "✅ Sent to Bridgy Fed or target" \
              || {
                echo "⚠️ Bridgy Fed failed, trying webmention.io..."
                curl -s -X POST https://webmention.io/wingpang.com/webmention \
                  -d "source=$POST_URL" \
                  -d "target=$TARGET" \
                  && echo "✅ Fallback to webmention.io succeeded" \
                  || echo "❌ Failed to send Webmention for $POST_URL"
              }

            echo ""
          done

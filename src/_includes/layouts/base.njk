<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title> {# Website title  #}
    <meta name="description" content="{{ description or metadeta.description}}">
    <meta name="generator" content="{{ eleventy.generator }}">
    <link rel="stylesheet" href="../../stylesheets/global.scss"/> {# Import styles #}
</head>
<body>
    {# Header #}
    {# Main content #}
    {# Footer #}
</body>
</html>